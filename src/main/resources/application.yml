server:
  port: 1234

spring:
  application:
    name: main-api
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=assessment
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  flyway:
    schemas: assessment

config:
  login-expire: 10 # minute